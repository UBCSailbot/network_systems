syntax = "proto3";

package Placeholder;  // TODO replace with new project name

message Sensors
{
    message Generic
    {
        uint64 id    = 1;
        bytes  data  = 2;
    }

    repeated Generic gen_sensors = 1;
}
