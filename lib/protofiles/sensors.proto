syntax = "proto3";

package Placeholder;  // TODO replace with new project name

message sensors
{
    message generic
    {
        uint64 id    = 1;
        bytes  data  = 2;
    }

    repeated generic genSensors = 1;
}
